import { Typography, Paper, Box, Divider } from "@mui/material";
import GestionControl from "../components/Fase3/GestionControl";

const Fase1Home = () => {
    return (
        <Box>

            <Box sx={{ mt: 6 }}>
                <GestionControl />
            </Box>
        </Box>
    );
};

export default Fase1Home;
